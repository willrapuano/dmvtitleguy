# Copy to .env.local and fill in values
# Server-only lead delivery. NEXT_PUBLIC_GHL_WEBHOOK_URL remains supported as a
# temporary compatibility fallback, but new environments should use this name.
GHL_WEBHOOK_URL=https://services.leadconnectorhq.com/hooks/YOUR_WEBHOOK_ID
