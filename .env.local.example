# Copy to .env.local and fill in values
# Rotate any webhook that was previously stored in a NEXT_PUBLIC_* variable.
GHL_WEBHOOK_URL=https://services.leadconnectorhq.com/hooks/YOUR_ROTATED_WEBHOOK_ID
# Generate with: openssl rand -hex 32
LEAD_PROTECTION_SECRET=replace_with_at_least_32_random_characters
