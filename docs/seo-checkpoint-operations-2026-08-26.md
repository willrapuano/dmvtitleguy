# DMV Title Guy SEO checkpoint operating contract

## Purpose and control state

This contract governs measurement after the August 26, 2026 release. Discretionary SEO publishing, title/meta/H1 changes, canonical changes, redirects, URL changes, authority outreach, directory edits, and profile changes remain frozen except for factual, legal, security, entity-truth, or broken-path corrections.

Will Rapuano operates `dmvtitleguy.io`. The public site is separate from Pruitt Title LLC's corporate website and does not bind Pruitt. The label `DMV Title Guy, LLC` in a GoHighLevel sub-account is operational metadata, not proof of the site's legal operator, DBA, data controller, or merchant of record. No public LLC claim or authority work may proceed until the owner confirms the legal name, DBA status, domain ownership, data controller, and permitted use of Pruitt references.

## Checkpoint calendar

| Date | Checkpoint | Permitted decision |
|---|---|---|
| September 2, 2026 | Technical | Inspect five priority URLs, sitemap state, Google/user canonical agreement, crawl/indexing state, public route health, and attribution operations. No SEO performance edits. |
| September 9, 2026 | Technical + final Week 1 archive | Repeat technical inspection, reconcile any non-QA delivery or GHL incident, and archive the finalized August 27–September 2 GSC window. This is descriptive only, does not substitute for the formal 28-day post window, and authorizes no SEO performance edits. |
| September 23, 2026 | Preliminary | Descriptive GSC and GHL readout only. Do not infer a completed post period. |
| September 26, 2026 or later | Final-data pull | Pull the August 27–September 23 post window with `dataState=final`. |
| September 30, 2026 | First eligible 28-day decision | Evaluate the fixed pre/post experiment if sample, position, and technical gates pass. |
| October 25, 2026 | Day 60 | Review path × cluster outcomes and only begin cannibalization analysis when 56 final days exist. |
| November 24, 2026 | Day 90 | Expand clusters that gained qualified visibility or business outcomes; do not expand from impressions alone. |
| February 22, 2027 or later | Earliest 180-day attribution review | Earliest date a clean-attribution long-horizon claim can be evaluated. This is not a promised win date. |

## GSC experiment and privacy contract

- Exclude August 26 as a washout day.
- Pre window: July 29–August 25, 2026.
- Final Week 1 descriptive window: August 27–September 2, 2026. It may not be captured before September 9 and is never decision-eligible.
- Post window: August 27–September 23, 2026.
- GSC source dates use `America/Los_Angeles`. Do not claim an Eastern-time daily join.
- Join only at period or week × normalized landing path × fixed cluster. Never join a person or GHL contact to a search query.
- Report total property metrics separately from visible-query coverage. Within visible queries, report owned brand, partner brand, competitor brand, visible known non-brand, and residual/unclassified. Anonymous-query totals make an exact non-brand property total impossible.
- Freeze brand dictionary `v1-2026-08-26`: owned = DMV Title Guy, dmvtitleguy, Will Rapuano, William Rapuano and normalized variants; partner = Pruitt Title variants; competitor = Federal Title and FederalTitle variants.
- Raw query exports stay in ignored `private-seo/`. Committed artifacts contain aggregates plus hashes and request manifests, not query text.
- Resolve `GSC_SERVICE_ACCOUNT_PATH` from the workspace's existing private credential source before running a finalized capture. Never print, copy, or commit the credential. If it is unavailable, report the checkpoint as blocked; do not substitute browser scraping, a partial UI export, or preliminary data for the canonical archive.
- Finalized captures use explicit grains: property totals, dates, and queries are `byProperty`; pages and query × page routing are `byPage`. Page-grain totals must never be presented as property coverage.
- A canonical finalized capture fails closed unless the GSC completeness probe places the first incomplete date after the requested window, every expected Pacific date is returned, property and daily totals reconcile, the Git tree is clean, and the immutable private artifacts pass their hash and permission checks.
- Run the Week 1 archive on or after September 9 with `npm run snapshot:gsc-checkpoint -- --window post-week-1 --capture-name 2026-09-09-post-week-1-final`.
- Run the formal post archive on or after September 26 with `npm run snapshot:gsc-checkpoint -- --window post --capture-name 2026-09-26-post-final`. Passing capture validation does not itself authorize an edit; the first decision review remains September 30.

The first title/meta CTR decision requires complete 28-day pre and post windows, at least 4,500 impressions in each window (or a documented page-specific power analysis), at least 20 clicks in each window, average position stable within 2 positions, and all technical gates green. Otherwise report the result as underpowered and make no CTR-driven edit.

## Preliminary opportunity queue

The August 28 rolling snapshot is a provisional queue, not an edit authorization. Preserve Silver Spring, Herndon, Tysons, Falls Church, and Vienna as controls. At the September 30 gate, review McLean first, followed by the Virginia title-company-choice guide, the enhanced-versus-standard title-insurance guide, the property-survey guide, the Maryland title-insurance-cost guide, Leesburg, and Arlington. Re-rank this queue from the finalized Week 1 and 28-day evidence; never preserve its order merely because it appears here.

## SpyFu competitive contract

- Preserve the August 26 v1 scorecard. Normalized monthly captures use `docs/seo-scorecards/monthly/YYYY-MM.json` and reject duplicate data months unless a documented capture defect requires `--force`.
- Compare identical US requests for both domains. The chosen endpoints do not expose a device parameter, so no device claim is allowed.
- Keep full-domain scale separate from the fixed v1 universe.
- Full-domain test: DMV Title Guy must lead Federal Title's estimated monthly organic clicks by at least 10%.
- Fixed-universe tests: weighted reciprocal rank, weighted top-three share, weighted page-one share, unweighted keyword wins, and leadership in at least three of five fixed clusters.
- “Surpassed” requires every current test to pass for three consecutive distinct SpyFu data months, plus green release/attribution gates and non-declining trailing-90-day GSC and qualified-opportunity outcomes. Report exact gaps until then.
- National SpyFu and GSC data are not a true local rank grid. Treat jurisdiction pages, jurisdiction-modified queries, and jurisdiction-tagged qualified opportunities as hyperlocal proxies unless a separately approved local-grid system is established.

## Local-search measurement contract

- The local lane is governed by `config/local-search-measurement.json` and `docs/local-search-measurement-2026-08-27.md`. It remains separate from GSC CTR and the FederalTitle SpyFu scorecard.
- A row is protocol-valid only when it matches the frozen query × eligible-cell matrix, pinned coordinate/tolerance, stable target ID, business-hours daypart, Chrome device profile, fresh signed-out private session, locale, no-filter state, surface-specific scan depth, full result sequence, target open state, timestamp, and recorder-verified evidence manifest. Protocol-valid does not itself mean improved; compare only identical comparison keys.
- The August 27 user screenshot shows the target as the third listing visible in the captured sequence and immediately above Pruitt Title LLC. Because query and geo are unknown, it is not a controlled rank baseline.
- Use manual controlled observations; do not automate Google result scraping. Measurement does not authorize a GBP, directory, website, or brand edit.
- The primary weekly batch is 12 three-result Local Pack observations on Wednesday, 9:00–11:00 a.m. Eastern. Local Finder is a separate 20-result conditional surface and Maps is a separate diagnostic; never combine their ordinals. Missing/invalid samples block decisions. Require two complete baselines for direction and four complete like-for-like batches before optimization, absent an identity/eligibility incident.
- Preview a batch first, then persist a complete checkpoint exactly once with `npm run report:local-search -- --batch YYYY-Www --write`. Canonical batch files are immutable, reject overwrites, and provide the prior-week history used by the trend gate.
- GBP Performance metrics do not expose rank. Treat Search/Maps impressions, website clicks, call clicks, and direction requests as their own diagnostics; they include organic and Ads activity and must not be added to GSC totals.
- `CALL_CLICKS` is not a completed or answered call. `WEBSITE_CLICKS` is not a session or lead. Message/conversation availability is profile- and product-dependent and must be probed after authorized connection.
- GBP API Content defaults to in-memory/no storage. Google's limited cache allowance is only to improve the API project's performance, expires within 30 days, and does not permit manipulation or aggregation. Do not create a reporting warehouse or committed aggregate.
- The prepared website-button UTM convention is `google / organic / gbp / profile-website-button`; only that exact tuple classifies as `google-business-profile`. The classifier is production-active after communication-suppressed browser → server → Turso → GHL QA on August 27. Do not apply the URL to the public profile until practitioner eligibility, written Pruitt authorization, and separate data-sharing/edit authority are confirmed.

## Qualification and outcome contract

A human must triage each non-QA transaction-intent opportunity within two business days. Use one primary reason:

- `qualified-dmv-transaction`
- `missing-property-or-jurisdiction`
- `insufficient-contact-information`
- `education-only-or-nontransaction`
- `newsletter-only`
- `advertising-only`
- `out-of-area`
- `duplicate`
- `spam`
- `test`

Report Submitted → Qualified → Referred → Accepted → Closed/Won. Exclude `SEO QA Excluded = true` and ledger `isQa = true` from every volume, rate, value, and loss calculation. Until a cohort has at least 20 non-QA opportunities, report counts without conversion-rate conclusions. Cohort outcomes are reviewed at day 30, 60, and 90; mutable outcomes never rewrite acquisition fields.

## Cannibalization decision rule

Do not consolidate before 56 final days. A candidate pair must share a manually reviewed intent, each have at least 200 visible impressions, the secondary URL must hold at least 20% of shared impressions, URL leadership must alternate in at least three weeks, both URLs must rank in the top 20, and their visible-query-set Jaccard similarity must be at least 0.40. Consolidate only when the winner has at least twice the visibility and the other URL has no distinct conversion or backlink value.

## Incident thresholds

### Git-backed canary and rollback sequence

`config/seo-operational-health.json` pins `scheduler` to `github-actions` and has one explicit `rolloutPhase`: `disabled`, `canary`, or `permanent`. The checked-in preparation state is `disabled`. In that state, `checkpointDates` and `canaryDates` are empty. `checkpointCalendar` records the immutable measurement calendar but does not activate it. The production Vercel project must never schedule `/api/cron/seo-health`; `vercel.json` and `ops/vercel.cron-off.json` must remain deeply identical for this job in every phase.

1. Reconcile and deploy the current production baseline from a clean protected `main` commit before activating the health route. Do not promote a CLI-built or dirty worktree deployment.
2. Issue the dedicated Vercel control-plane integration credential and provider credentials, complete the scope evidence below, and pin every exact credential and target fingerprint. Blank Vercel integration IDs/slug, `vercelControlTokenSha256`, `databaseTokenSha256`, `ghlReadTokenSha256`, or `ghlTargetSha256` values intentionally block release.
3. Before the one-date trial, provision the independent scheduler watchdog described below, complete one recovery drill, and commit its signed receipt. For the trial, set `rolloutPhase` to `canary`, keep `checkpointDates` empty, and set exactly one valid future `America/New_York` date in `canaryDates` that does not overlap `checkpointCalendar`. Keep the Vercel health cron absent. The protected GitHub Actions workflow remains scheduled at `17 12 * * *` and must exit without provider calls on every non-allowlisted date. A canary is accepted only from the `schedule` event; `workflow_dispatch` is rejected by the secretless gate and again by the run-identity gate. Deploy the configuration only through a reviewed GitHub PR into `main`.
4. On the canary date, accept the run only after the production-target, Git-source, runtime-binding, completeness, zero-incident, provider-credential, and durable-archive checks below all pass. A 2xx route status by itself is not sufficient evidence.
5. If the canary is incomplete or unhealthy, set `rolloutPhase` back to `disabled`, empty both active date sets, and make a clean forward deployment. Verify that `vercel.json` still deep-equals `ops/vercel.cron-off.json` and preserves the GHL reconciliation cron. Disablement is configuration-driven; do not assume an instant rollback changed the workflow or active date set.
6. Only after a fully archived canary passes, set `rolloutPhase` to `permanent`, empty `canaryDates`, copy the complete `checkpointCalendar` into active `checkpointDates`, retain the GitHub Actions `17 12 * * *` schedule, rerun the full release suite, and merge through protected `main`. The permanent change may alter only rollout state and evidence references: the canonical `healthSourceDigest` computed from the allowlisted CI and runtime workflows, critical static release verifier, runner, archive, attestation, transitive local health modules, dependency manifests/lockfile, Next/Vercel manifests, and normalized configuration sources must exactly equal the digest signed into the canary archive. Any executable health-source, key, provider-scope, control-plane, dependency, or workflow change requires a new scheduled canary. The Vercel health cron remains absent.

Permanent activation requires an exact `canaryReceipt` with only `checkpointId`, `scheduledDate`, `finishedAt`, `commentId`, `commentBodySha256`, `deploymentFingerprint`, `evidenceDigest`, `healthSourceDigest`, and `githubSha`. Copy those values from the canonical v2 archive and the authenticated GitHub API; never synthesize them. All remote receipt authentication runs inside GitHub Actions with the automatically minted, job-scoped `GITHUB_TOKEN`: the minimal `CI / configured-receipt` job and every due operational-health run receive only Actions-read, Contents-read, and Issues-read. They fetch the exact issue #47 comment by `commentId` and the immutable Actions attempt. The Vercel Production build performs only static configuration, source-digest, receipt-shape, and elapsed-history-completeness validation; it never calls GitHub. No personal access token, fine-grained personal token, Vercel runtime GitHub token, or anonymous GitHub API quota is an acceptance path. Acceptance requires the exact `github-actions[bot]` login, numeric ID `41898282`, `Bot` type, repository/issue/API/web URLs, canonical v2 marker and body, body hash, canary identity, timestamps, deployment/evidence/health-source digests, and pinned 40-character commit SHA. The canonical v2 body must also carry an `seo-operational-health-signature-v1` Ed25519 signature bound to that exact sanitized body, repository, and issue. `archiveSignature` pins the `Ed25519` algorithm, SHA-256 public-key ID, and canonical SPKI-DER public key in base64. The matching canonical PKCS#8-DER private key is a one-line base64 protected-environment secret named `SEO_HEALTH_ARCHIVE_SIGNING_PRIVATE_KEY`; only the archive process may receive it, and it must never be placed in Vercel, printed, committed, or passed to the date, pre-attestation, or provider processes. The verifier requires the immutable Actions attempt endpoint `/actions/runs/{run_id}/attempts/{attempt_number}`, not the mutable latest-attempt run endpoint, and requires this exact workflow path on `main`, a scheduled event for the canary, matching attempt and head commit, a completed successful conclusion, and an unedited comment created inside that attempt window. Deletion, editing, redirection, rate limiting, unavailable APIs, an invalid signature, or any schema/provenance mismatch fails closed.

If the archive signing key is rotated, first return rollout to `disabled`, remove the accepted canary receipt, generate a new Ed25519 keypair outside the repository, replace the protected-environment private key and checked-in public-key pin as one reviewed change, and run a new scheduled canary. Never use an archive signed by the retired key to activate the new key. The current verifier intentionally supports one active public key; once `checkpointHistory` contains evidence signed by that key, a direct replacement would make that history unverifiable and block permanent rollout. If post-checkpoint rotation becomes necessary, keep rollout disabled and first ship a reviewed multi-key historical-verification schema under a new scheduled canary. Preserve retired public keys only in that explicit verification keyring and external audit records; never retain retired private keys in the repository or Vercel.

The isolated GitHub Actions workflow runs at `12:17 UTC`. A secretless job resolves the active date allowlist, evaluates elapsed-history completeness and signed-watchdog freshness, and verifies any required accepted canary or historical evidence before any credential-bearing job. It emits only safe `controls_ready` and failure-code outputs. Pre-attestation, provider, and archive skip when controls are not ready; a checkpoint-only fallback may receive only the archive signing key and job-scoped GitHub token to record the failure. Receipt/API failure makes the secretless job fail, so no secret-bearing job, including fallback, starts. Disabled performs no receipt network request; canary authenticates any existing history references but has no canary receipt. This places its terminal record before the existing 9:00 a.m. Eastern checkpoint recorder in both daylight and standard time without changing the fixed checkpoint date. Main-workflow `workflow_dispatch` exists only for an owner-initiated permanent-phase checkpoint recovery while the effective Eastern date is still allowlisted. The secretless gate and every credential-bearing process require the exact `willrapuano` login and immutable actor ID `200251753` for a manual dispatch; it can never create or replace a canary.

### Owner-only GitHub authorization checklist

The `main-pr-only` ruleset and explicit owner approval are required before this bootstrap PR may merge. The protected environment, integrations, and credential-name configuration may remain unprovisioned until canary. These are repository-owner/admin operations; neither CI nor an implementation agent may substitute for the settings evidence.

- In **Repository → Settings → Rules → Rulesets**, create or update an active branch ruleset named `main-pr-only` whose stored target is exactly `refs/heads/main`. In **Add target → Include by pattern**, enter only `main`: GitHub adds `refs/heads/` internally. The visible branch pattern must be `main`, not `refs/heads/main`; typing the full ref produces the incorrect stored target `refs/heads/refs/heads/main`. After saving, verify that the rule applies to `main` through GitHub's effective branch-rules API, not merely that the ruleset is Active. Leave the bypass list empty. Block deletion and force pushes. Require a pull request, one approving review, dismissal of stale approvals, approval of the most recent reviewable push, Code Owner review, and resolution of every conversation. Require the branch to be current before merge and require these literal status-check contexts: `configured-receipt` and `verify` from **GitHub Actions** (integration ID `15368`), plus `Vercel` from **Vercel** (integration ID `8329`). The first two jobs belong to the `CI` workflow and may appear as `CI / configured-receipt` and `CI / verify` in grouped displays; `CI /` is not part of the stored check context. These exact contexts and sources were confirmed by GitHub's ruleset and effective branch-rules APIs on September 2, 2026. If an integration later reports a different literal context or source, stop and update this reviewed checklist to that observed value before configuring the rule. Do not guess, select a broad wildcard, or permit owner, administrator, bot, or integration bypass.
- Confirm `.github/CODEOWNERS` is present on protected `main`, resolves `@willrapuano`, and covers `.github/workflows/`, the health configuration, health runtime and verifier modules, the source-digest implementation, the Production environment gate, and the attestation endpoint. Open a test PR touching one protected path and confirm GitHub automatically requests that Code Owner and blocks merge until approval.
- Because `.github/CODEOWNERS` is introduced by this bootstrap PR and cannot govern itself from the base branch, explicitly approve this PR as owner before merge; do not treat the bot-authored PR or ordinary CI success as Code Owner approval.
- In **Repository → Settings → Environments → `seo-health-production`**, set **Deployment branches and tags** to **Selected branches and tags**, add only `main`, and add no tag rule. Set required reviewers to none and the wait timer to `0` so the scheduled workflow remains unattended. Disable administrator bypass. Store only these environment secrets: `SEO_HEALTH_ATTESTATION_SECRET`, `SEO_HEALTH_VERCEL_CONTROL_TOKEN`, `SEO_HEALTH_TURSO_DATABASE_URL`, `SEO_HEALTH_TURSO_AUTH_TOKEN`, `SEO_HEALTH_GHL_READ_TOKEN`, `GHL_LOCATION_ID`, `GHL_WEBSITE_PIPELINE_ID`, `GHL_WEBSITE_SUBMITTED_STAGE_ID`, and `SEO_HEALTH_ARCHIVE_SIGNING_PRIVATE_KEY`. `GITHUB_TOKEN` is job-scoped and automatic; never add it as a secret.
- In **Vercel → dmvtitleguy → Settings → Environment Variables**, remove `SEO_HEALTH_GITHUB_READ_TOKEN` from Production, Preview, Development, and every branch override. It is forbidden in the site environment. While rollout is `disabled`, `SEO_HEALTH_ATTESTATION_SECRET` may be absent; if present, it must already contain at least 32 random bytes. Before changing to `canary` or `permanent`, add the same at-least-32-byte attestation value to Vercel Production and the GitHub `seo-health-production` environment. Never add the archive key, Vercel-control token, or provider health credentials to Vercel.
- Preserve owner evidence showing the active ruleset name and target, empty bypass list, exact required checks, protected-environment branch selector, secret **names only**, and the successful Code Owner test. Do not capture or store secret values.

The absence of environment reviewers is deliberate only because the exact workflow/ref checks, owner-reviewed PR-only `main`, empty bypass list, and selected-branch environment restriction form the authorization boundary.

### Independent scheduler continuity

GitHub documents two hazards that the repository cannot self-heal: a [scheduled workflow in a public repository can be disabled after 60 days without repository activity](https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule), and a scheduled run can be delayed or dropped. GitHub also documents the separate [workflow re-enable operation](https://docs.github.com/en/actions/how-tos/manage-workflow-runs/disable-and-enable-workflows). A second workflow in this repository is not independent and does not satisfy this control. Before canary, provision an owner-controlled external GitHub App watchdog whose installation is limited to this repository and whose permissions are exactly Actions read/write plus metadata read. Pin SHA-256 hashes of the monitor, GitHub App, installation, repository ID, and exact workflow path, plus a dedicated Ed25519 receipt-verification public key. It must poll the exact workflow state at least hourly, re-enable that workflow when GitHub reports it disabled, and alert the owner when no scheduled run appears within the configured hour. It must never dispatch a canary. A dropped canary is rescheduled to a new future date. A dropped permanent checkpoint may be manually dispatched only on the same Eastern date; otherwise it becomes a signed missed-checkpoint incident.

Self-asserted capability booleans are not evidence. The external service must sign the canonical `seo-health-watchdog-receipt-v1` payload, binding its pinned identities, repository/workflow, exact permissions, observed active state, and real disable/detect/re-enable/alert drill timestamps. Canary and permanent fail closed if the signature is invalid, the receipt is more than 168 hours old, the drill is more than 30 days old, detection exceeded 60 minutes, or recovery exceeded 24 hours. The checked-in disabled bootstrap intentionally leaves external pins, public key, and receipt blank; the aggregate release verifier still passes in this inert state, but canary/permanent do not. The signature and pin checks are enforceable now; provisioning the external GitHub App, retaining its private receipt key outside this repository, and arranging at-least-weekly signed-receipt publication remain owner-operated live steps. Rotate a watchdog receipt key only while disabled, update the pin, and run a new scheduled canary because the key/pin changes the canonical health source.

#### Independent Vercel control-plane credential evidence

Create a dedicated Vercel REST API Integration named for this one checkpoint control. Install it on the pinned team with project selection `selected` and only the exact `dmvtitleguy` project. In the Integration Console, grant **Read**—never Read/Write—for only `integration-configuration`, `deployment`, `project`, and `domain`; every other scope remains None. Use the installation's OAuth access token, not a personal/account token. The API must return exactly that one pinned installation from Vercel's authenticated-integration configurations endpoint, and the direct installation lookup must agree with it. Vercel's current integration-configuration response exposes effective scopes with `read:` or `read-write:` prefixes, so pre-attestation and archive require the exact set `read:integration-configuration`, `read:deployment`, `read:project`, and `read:domain`. Any `read-write:` scope, extra scope, additional/mismatched authenticated installation, all-project installation, other project, disabled/uninstalled configuration, or unrecognized response fails closed. See Vercel's [API Integration scope reference](https://vercel.com/docs/integrations/create-integration/vercel-api-integrations#scopes), [authenticated-integration configurations endpoint](https://vercel.com/docs/rest-api/integrations/get-configurations-for-the-authenticated-user-or-team), and [direct integration-configuration endpoint](https://vercel.com/docs/rest-api/integrations/retrieve-an-integration-configuration).

Store the token only as the protected GitHub environment secret `SEO_HEALTH_VERCEL_CONTROL_TOKEN`. It may enter only the pre-attestation and archive processes; it must be absent from the secretless gate, provider process, Vercel Production environment, logs, bundles, issue comments, and repository. Pin these non-secret values in `deploymentBinding.vercelControlPlane`: `teamId`, `integrationConfigurationId`, `integrationId`, `integrationSlug`, `projectSelection: "selected"`, canonical alias `dmvtitleguy.io`, and the exact four effective read scopes. Hash the exact token locally into `deploymentBinding.fingerprints.vercelControlTokenSha256` without printing or committing the token.

Before transmitting the deployment-attestation secret, both credential-bearing control processes use authenticated GET requests to independently require: the pinned active integration installation and read-only scopes; the alias record `dmvtitleguy.io` bound to the exact attested deployment and project; the exact `READY` production deployment, team, Git SHA/ref/repository metadata, and canonical alias; the deployment's alias list containing that canonical alias exactly once; and the verified, non-redirecting project-domain assignment. The archive process repeats the proof and requires its sanitized digest to equal pre-attestation. Token or integration rotation is a control-plane change: disable rollout, replace the protected secret and all pins as one reviewed change, and run a new scheduled canary.

#### Provider credential evidence

The health runner requires dedicated provider credentials. Credential scope remains provider-enforced least privilege. The provider process receives only those read credentials, the pinned GHL target IDs, and a sanitized pre-attestation bundle. It never receives the attestation credential or GitHub token. The pre-attestation and archive processes never receive provider credentials, and the production Vercel project must never receive `SEO_HEALTH_TURSO_DATABASE_URL`, `SEO_HEALTH_TURSO_AUTH_TOKEN`, or `SEO_HEALTH_GHL_READ_TOKEN`.

- Turso: issue a bounded token whose lifetime is no more than 210 days with three explicit permissions—`LeadSubmission:data_read`, `LeadOpportunityOutbox:data_read`, and `LeadSubmissionEvent:data_read`—and no `all`, broad `--read-only`, data-write, or schema action. For example, use `turso db tokens create <database> -p LeadSubmission:data_read -p LeadOpportunityOutbox:data_read -p LeadSubmissionEvent:data_read` plus the approved expiration option. The runner recognizes only Turso's documented fine-grained JWT permission structure and requires its exact semantic grant set; an empty/all table selector, any write action, any other table, legacy broad claim, opaque claim, duplicate, or unrecognized claim fails closed before network access. Pin the exact non-secret `iat` and `exp` metadata in `credentialPolicy.turso`; the token must remain valid through at least `2027-02-24T12:17:00.000Z`, which is the final scheduled checkpoint plus the required 48-hour safety margin. Using that same token, live read probes for all three allowlisted tables must succeed while a read probe for unrelated `LeadRateLimitBucket` must fail authorization. The successful provider calls establish that Turso accepted the exact claim-bearing token; the denial proves the negative boundary. Do not use a mutation as a negative probe. Hash the exact credential locally into `databaseTokenSha256`; never print, commit, or archive it. See Turso's [fine-grained permission format and actions](https://docs.turso.tech/sdk/authorization/fine-grained-permissions).
- HighLevel: create a **Sub-Account/Location-level** private integration named `DMVTitleGuy SEO Health Read-only` with exactly `locations.readonly`, `opportunities.readonly`, and `locations/customFields.readonly`. Preserve provider-UI evidence showing only that integration name, Location binding, and exact scope set, never the token. The runner requires the provider-issued JWT to carry `authClass: "Location"`, `authClassId` equal to the pinned location, and `oauthMeta.scopes` equal to that exact unique set. A Company token, wrong Location, extra/write scope, missing scope, duplicate, opaque claim, or unrecognized claim fails closed before network access. Using that same token, positive reads for the bound location, opportunity pipeline/search, and opportunity custom fields must succeed; a safe read that requires ungranted contact scope must fail authorization and its body is discarded. The successful provider calls establish that HighLevel accepted the exact claim-bearing token; the denial proves the negative boundary. Hash the exact credential locally into `ghlReadTokenSha256`; never print, commit, or archive it. See HighLevel's [Private Integration guidance](https://marketplace.gohighlevel.com/docs/Authorization/PrivateIntegrationsToken/index.html) and [scope reference](https://marketplace.gohighlevel.com/docs/Authorization/Scopes/).
- Pin the combined location, pipeline, Submitted-stage, SEO Submission ID-field, and SEO QA Excluded-field target digest as `ghlTargetSha256`. The provider evidence, positive/negative probe outcomes, credential digests, and expiry belong in the sanitized checkpoint archive; raw provider responses and identifiers do not.

While rollout is `disabled`, the Production site build accepts an absent `SEO_HEALTH_ATTESTATION_SECRET`; a present value must contain at least 32 characters. The endpoint remains deployed but fails closed when that secret is absent or malformed. Canary and permanent Production builds require a valid at-least-32-character attestation secret, and the matching value must be present in the protected GitHub Actions environment before any due run. The Production gate forbids `SEO_HEALTH_GITHUB_READ_TOKEN`, the archive signing key, Vercel-control token, and provider health credentials in the site environment. It performs no remote receipt fetch. The static release verifier still requires all Vercel-control identifiers/token digest and provider credential/target digests before canary or permanent release, while authenticated receipt provenance is checked by the minimal CI job and the operational-health runtime with their job-scoped `GITHUB_TOKEN`.

Every dated checkpoint must be archived within 24 hours of its terminal `finishedAt` as a sanitized comment on GitHub issue [#47](https://github.com/willrapuano/dmvtitleguy/issues/47). Terminal start and finish dates are compared in `America/New_York`, not by their UTC string prefix, so a valid late-evening Eastern run remains attributable to the intended checkpoint after UTC midnight. The comment timestamp must be unedited, inside the immutable Actions attempt window, and no more than 24 hours after `finishedAt`. Before accepting or archiving a healthy terminal record, the recorder must independently verify through the dedicated read-only Vercel integration that:

- the authenticated integration configuration is active, owned by the pinned team, selected for only the pinned project, and exposes exactly the four `read:` scopes with no write or extra scope;
- the alias record, deployment-alias list, and project-domain record all bind `dmvtitleguy.io` to the same exact `READY` production deployment and project without a redirect;
- the deployment is from the pinned GitHub repository ID, owner, and slug on `main` with a full commit SHA and `gitDirty` not equal to `1`;
- the authenticated deployment-attestation response binds the workflow run to that exact provider-attested deployment fingerprint;
- one terminal structured record exists in the isolated workflow run for the exact checkpoint ID and scheduled date, with `healthy=true`, `complete=true`, valid evidence and deployment digests, every sanitized binding true, zero incident codes, and `seoChangeAuthorized=false`.

The issue comment may contain only the fields allowlisted in issue #47. It must omit tokens, headers, raw URLs or provider responses, CRM/database identifiers, contact or property information, ledger/GHL aggregates, row counts, stack traces, and raw errors. The archive job records an incident when it can; a separate minimal `incident_fallback` job runs after archive and, on a checkpoint whose controls, pre-attestation, provider, or archive job failed/cancelled/timed out, reuses or writes the same idempotent signed `seo-operational-health-incident-v1` record. The fallback receives no provider, Vercel-control, or attestation credentials, and deliberately leaves its Actions attempt non-successful after recording. It never runs when the secretless schedule/receipt gate itself failed. A missing, late, malformed, duplicated, unarchived, or provider-unattested record is incomplete evidence and cannot authorize an SEO change. If GitHub itself is unavailable or the entire workflow is administratively cancelled before the fallback can start, the structured workflow failure is the only immediate record and owner recovery is required. A later GitHub Actions attempt cannot turn the prior attempt's terminal bundle into accepted evidence because its comment would fall outside the original immutable attempt window; rerunning the failed archive job can therefore record a signed missed incident, but it cannot upgrade the earlier terminal result to an accepted archive. There is deliberately no next-day provider replay path.

For an elapsed checkpoint missed while rollout was `disabled`, `canary`, or `permanent`, wait until the **entire following Eastern calendar day** has elapsed, then dispatch `SEO operational health missed-checkpoint recovery` from current protected `main`. The current rollout phase does not have to match the historical phase in which the checkpoint elapsed. Only `willrapuano` with GitHub actor ID `200251753` is accepted. Enter the exact date and ID from `checkpointCalendar` and choose the allowlisted reason matching the rollout phase at the time of the miss. The recovery script authenticates the immutable current Actions attempt, protected `main` branch and head SHA, repository identity, workflow/ref/attempt, and actor; it signs the actual current detection time and never calls a provider or accepts a backdated timestamp. It idempotently reuses only an incident whose original recovery attempt completed successfully; a cancelled or failed prior attempt cannot poison replacement. Copy the emitted safe `checkpointHistory` reference into a reviewed `main` PR. The permanent/canary verifier later authenticates the public signed comment and the successful immutable recovery-workflow attempt, including the owner identity. This process records a miss; it never replays a provider or turns the miss into a pass.

`checkpointHistory` must account for every checkpoint whose full following Eastern recovery day has elapsed before canary or permanent controls become ready. Each entry is either an authenticated, signed accepted-archive reference or an authenticated, signed missed-checkpoint incident reference. Permanent activation fails closed when any required date is absent or when the referenced public comment, immutable attempt, signature, identity, digest, or timing does not verify. A missed incident preserves truth; it never converts the checkpoint into a pass.

Run the health gate only through the protected GitHub Actions workflow. Before transmitting the attestation credential, the pre-attestation process must verify the exact repository, workflow path, `main` ref, commit SHA, and literal production origin. It must bind the attested deployment first to the latest successful `Production` deployment status created by the pinned Vercel bot identity, then independently bind the canonical alias/project/deployment through the dedicated read-only Vercel integration, and only then contact the unique deployment hostname with the attestation secret. The provider process consumes only the sanitized run-bound bundle and dedicated read credentials; `@libsql/client` is loaded only after every local identity, replay, claim, and credential-boundary gate passes. The archive process repeats the attestations, GitHub deployment provenance, and Vercel control-plane proof after provider calls before recording allowlisted terminal evidence. If any required secret, digest, exact provider claim, attestation, provenance record, or provider response is unavailable or unrecognized, the workflow must fail closed. Never copy provider health credentials into Vercel, downgrade a sensitive variable, print raw environment output, or treat an incomplete GHL opportunity page as a green reconciliation.

Treat these as P0/P1 incidents, not SEO experiments:

- any non-QA webhook delivery in `unknown`;
- `sending` older than five minutes;
- delivered transaction-intent GHL sync pending over ten minutes or in error;
- a confirmed-delivered non-QA transaction missing its ledger-linked opportunity, a contact UUID mismatch, or a newest submission whose reused GHL card does not expose that exact submission ID;
- any priority URL returning non-200, `noindex`, a wrong canonical, or missing from the sitemap;
- sitemap errors or warnings;
- an unexpected `www` or HTTP alias response.

The recovery worker may retry only the idempotent GHL opportunity sync after confirmed webhook delivery. It must never replay an ambiguous webhook.

## Retention

- Encrypted GHL opportunity outbox: maximum 14 days; expiration creates an append-only manual-reconciliation event and deletes the encrypted payload.
- Pseudonymous acquisition ledger and append-only event hashes: 24 months, then retain aggregate reporting and remove row-level identifiers unless an active transaction, dispute, or legal hold requires longer.
- GHL contact/opportunity PII: review for deletion or anonymization 24 months after the last meaningful activity unless an active transaction, legal obligation, documented consent, or legal hold applies.
- Controlled QA: keep the single reserved DND QA contact and explicit QA tag/field; remove obsolete QA opportunities after 30 days once reconciliation evidence is preserved.
- Raw GSC query exports: private, access-limited, and retained 13 months for year-over-year analysis, then deleted. Committed aggregates and hashes may remain.
- Private controlled-observation screenshots: retain 13 months, then delete unless needed for an active dispute or documented longitudinal audit; committed observation metadata and hashes may remain.
- Any authorized GBP API-provided Content: in-memory/no storage by default. A secure, unmanipulated, unaggregated cache may exist only to improve the API project's performance and must be deleted no later than 30 days after retrieval.

No destructive retention action is automated by this release. The owner must approve the first deletion run after confirming legal and operational retention requirements.

## Authority permission matrix

| Surface/action | Current permission state | Required evidence before action |
|---|---|---|
| DMV Title Guy website content | Will-controlled | Entity-truth and release gates green. |
| Will-controlled LinkedIn or social profile | Frozen | Owner confirms operator/DBA language and approves exact profile copy. |
| Pruitt website, GBP, directory, social, logo, testimonials, awards, volume or licensing claims | Blocked | Written authorization from the appropriate Pruitt owner plus source evidence. |
| Manual observation of publicly visible local results | Measure-only | Exact protocol context and evidence; no automated scraping, profile mutation, control claim, or ownership inference. |
| GBP read access | Blocked pending proof | Stable target identity, practitioner eligibility review, and verified Owner/Manager read authority. |
| GA4 ↔ GBP data-sharing link | Blocked pending proof | Explicit data-sharing authorization from the appropriate business owner in addition to GA4 and GBP roles. |
| Pruitt-associated GBP website/name/hours edit | Blocked | Practitioner eligibility plus written Pruitt-owner edit authorization; Owner/Manager access alone is insufficient. |
| Association, CE instructor, brokerage-resource, podcast, or partner outreach as DMV Title Guy | Frozen | Operator identity confirmed, exact sender identity approved, relationship basis documented, no compensation or reciprocal-link scheme. |
| Directory/GBP creation at a Pruitt address | Prohibited without authorization | Written location/brand authorization and compliance review; never infer permission from employment or a GHL label. |

No external outreach, profile edit, directory edit, or Pruitt-controlled change is authorized by this contract.
